services:
  - type: web
    name: onenet-relay
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: ONENET_TOKEN
        value: mytoken
      - key: FORWARD_URL
        value: ""
